{
  "name": "react-with-wasm",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/react-app",
    "packages/wasm-module",
    "packages/hello-wasm"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "build:wasm": "pnpm --filter wasm-module run build",
    "dev": "pnpm --filter react-app run dev"
  }
}
